# 主管理：这台电脑与它下面所有项目的长期约定

`D:\AI` 是**主管理**（管家）：管这台电脑上所有项目的公共部分；它自己也带一个项目，就是本机的配置与维护。
项目一律放 `D:\Projects\<名字>\`，各自维护自己的 `状态.md`，并在 `项目登记表.md` 里登记一行。

对话可能被压缩、也可能开新会话，所以**状态一律以文件为准，不依赖对话记忆**。

## 最小必读（上下文紧张、或只想快速开工时，至少读这一段）

1. **开工**：读 `任务清单.md`（总览）→ 读 `状态.md`（现状）→ 打开对应的 `任务\T-*.md` →
   用户已选中的任务先建同 ID 的 `进行中\T-*.md` 认领，再动手
2. **红线**：数据放 D 盘 · 密钥不进任何仓库 · **删东西之前先问用户** · 不许动别人认领的行
3. **收工**：更新或移除自己的任务文件和认领卡 → 重新生成 `任务清单.md` →
   用专用状态锁更新 `状态.md`；理由追加到 `决策记录\<当月>.md`，踩过的坑写进 `知识库\`
4. **杂活自己干，不要问**：归类、整理、清理过期物、归档脚本、更新索引
5. **六类事先问用户**：花钱 · 删除/不可逆 · 改偏好 · 对外发布/上传/发送 · 登录/密钥/权限 · 重启/安全策略/开机项/后台服务

## 谁维护什么（重要前提）

**这套东西是给会话用的，用户不会细看**，他只有三件事：给决定、给钱、确认删除。

- 别写"请用户记得……"这类约定——他不会记得，那条约定等于不存在
- 归位、归类、清理、归档、索引、日志，全部由会话做
- 发给用户的话要短：做成了什么、我能感知到什么、要我做什么
- 问他时**必须带默认方案**（"我打算这样，行不行"），不要让他从零想。
- **但默认方案只在"开口问他"时用**：任务写明「等用户选定／等用户拍板」的，**选定之前一步都不许动**——
  用户说「开始」只等于开始准备（调研、核实、把选项和代价摆出来），**不等于替他选了**；
  装软件、删东西、改设置尤其算。这类任务要在任务文件里写 `- 需用户先定：是`，
  生成的总览会把它标成「❗等你定」。

## 每次开工

1. 读 `任务清单.md`：有什么挂着、哪件等用户拍板、哪件已被别的会话认领。
2. 读 `状态.md`：本机现在是什么样。
3. 要做的事不在清单里 → **先建任务文件再动手**（格式与命令见 `知识库\治理体系.md`）。
4. 涉及具体步骤读 `决策记录\` 当月条目；想知道「怎么做」读 `知识库\`（按主题分册）。
5. 动手前确认权限边界，见 `状态.md` 的「已知问题」。

## 任务与并发（多个对话同时干活）

- **事实源是 `任务\` 里的逐任务文件**：一项任务一个稳定 ID，不同会话写不同文件；`任务清单.md` 只是生成的总览，不手改。
- **认领靠同 ID 卡片**：动手前在 `进行中\` 原子创建 `<任务ID>.md`；建成功才算认领，已存在就不能接手。只改自己的任务文件和卡。
- 等用户或外部条件时，把任务状态改成 `等用户`/`阻塞`（因为要等，不许拿卡占着）并**删掉认领卡**。
- **`状态.md` 单独加锁**：写前原子创建 `进行中\共享-状态.md`，写完立刻删；锁已存在就把结果写进自己的卡，稍后并入。
- 任务从哪来、谁决定做什么、模板与命令：见 `知识库\治理体系.md`。

## 项目层级与指路

- 主管理定**红线**：数据放 D 盘、密钥不进任何仓库、不许自作主张改安装位置、不许动别人认领的行。
- 项目可以定更细的约定，但**只能更严，不能更松**。
- **每个项目的 `AGENTS.md` 第一行必须是「先读 `D:\AI\AGENTS.md`」**——项目不在 `D:\AI` 目录树下，
  不写这一句，Codex 在项目里就不会读到主管理约定（除非直接指定 `D:\AI`）。
- 新建项目：先建目录和自己的约定文件，再在 `项目登记表.md` 加一行（名字 / 路径 / 一句话说明 / 公开还是私有）。

## 每完成一个任务

- `任务\`：完成后删除自己的任务文件和认领卡；未完成则更新状态、最后更新和下一步
- `任务清单.md`：运行生成脚本刷新总览；做完的任务不保留在总览
- `状态.md`：取得 `共享-状态.md` 锁后把「现在」更新成最新——只写新现状，不写流水账；写完释放锁
- 有值得留住的取舍或理由，追加到 `决策记录\<当月>.md`（`决策记录.md` 只是索引）
- **用户说「不做 / 取消」也是收工**：删掉任务文件 → 在 `状态.md` 的「已取消 / 已放弃」记一行（带日期与理由）
  → 追加决策记录 → **立刻重跑生成脚本**。反过来，看到 `状态.md` 引用某个任务而文件不在时，
  **先查「已取消」段和决策记录**，确认不是用户否决过，再补建。

## 文件体积控制

- `状态.md` 只写「现在」，**超过 120 行就先压实**：留结论和还在生效的事；
  「已取消 / 已放弃」「已知问题」里变成背景的可以压，机器与系统、硬盘健康、软件与开发环境、
  目录与数据这些「现状」不许压掉。
- `决策记录\` 只追加、不删；控制手段是「每条写得短」。

## 写作规则

- `状态.md` 只写「现在」，不写流水账；历史交给 `决策记录\<年-月>.md`。
- `决策记录\` 只追加：要推翻旧结论就新增一条并注明推翻了哪条，不改旧条目。
- **不写密钥、令牌、账号密码**（这台机器上有 API 中转凭据，尤其注意）
- 不粘对话原文（原文在 `.codex\sessions\`）；每个文件保持简短；这里只有一个副本，不要在别处再建一份。

## 知识库（经验教训）

- 位置 `D:\AI\知识库\`，**一个主题一个文件**（`Docker.md`、`PowerShell.md`、`治理体系.md`…），可以改写、合并、改错。
- 收录「下次还会用到」的坑与做法；分工：决策记录写「为什么这么定」，知识库写「怎么做」。

## 产物存放与目录职责

- 主管理产物放进 `D:\AI` 的分类目录；具体项目产物放回对应的 `D:\Projects\<项目>`，不在两处各留一份
- 治理文件留在根目录：`AGENTS.md`、`状态.md`、`任务清单.md`、`项目登记表.md`、`安装规划.md`、
  `决策记录.md`（索引）
- 根目录另外允许放**指向外部目录的快捷方式**（如 `项目目录（D-Projects）.lnk` → `D:\Projects`），
  方便在资源管理器里跳转；这类入口不算治理文件
- 下载的安装包不算产物，可以留在下载目录

目录职责（哪个目录放什么）见 `知识库\治理体系.md`。**不属于 `D:\AI` 的三类东西**别往这里塞：

- 你的个人文件（文章、资料、图片收藏）→ `D:\Documents`
- 具体项目的东西 → `D:\Projects\<项目>\`
- 备份 → `D:\Backups`

判断不了放哪，先扔 `收件箱\`，下次开工顺手归类。

### 保留期（只提示，不自动删）

**过期 ≠ 该删**：到期只报给用户看，删不删他说了算。期限：`进行中\` 24 小时、`产物\logs\` 30 天、
`收件箱\` 30 天、`文档\` 90 天；细节见 `知识库\治理体系.md`。

## 删除的红线

动删除之前先分清自己有没有资格：

- **我自己刚产生的临时文件**（探针脚本、当次操作的日志）→ 可以直接删
- **用户的东西、来历不明的东西** → **先问，不要替用户决定**
- **有保留期的东西**（进行中卡、收件箱、日志）→ 只报不删，等用户拍板

理由：这是用户的机器和数据，"过期"只说明可疑，不等于该消失。

## 复用库（值得留到第二次的东西）

- **同一件东西用到第二次才收进库**；一次性的不进（探针、临时诊断用完就归档或删）。
- 入库的在 `工具\工具箱.md` 占一行；三个月没用到的降级或删掉。

## 公开与私有

- Git 的公开/私有结构**还没定**，在此之前不要初始化、提交或发布仓库（见任务 T-20260918-03）。
- **密钥、令牌、账号密码一律不进任何 git 仓库**（私有仓也不行）；`状态.md`、`决策记录\`、
  `文档\重装清单.md` 和含账号/地址的资料不得进公开仓。值放密码管理器（KeePassXC）。
- 建仓时的白名单思路与细节：见 `知识库\治理体系.md`。

## 安装规则（装软件时必看）

- **装什么、从哪下载、密钥与订阅**：以 `D:\AI\文档\重装清单.md` 为准
- **装到哪**：以 `安装规划.md` 为准。清单只决定装什么，位置不由清单决定
- 顺序是硬性的：先 Clash Verge 并导入订阅，能上网了再装其余软件
- 装完一个就在 `状态.md` 记一行，并确认它的数据目录、库目录已经指向 D 盘
- 不要自作主张更换安装位置。要改先跟用户确认，改完同步更新 `安装规划.md`

## 本项目的既定约定（不要随意改动）

- C 盘（NVMe 512G）= 系统、软件、运行中的程序；D 盘（SATA 512G）= 数据、记录、备份
- 重装系统只清 C 盘，所以任何不能丢的东西都必须在 D 盘
- AI 主力走云端 API，不在本机跑大模型（显存 6GB、内存 16GB 是硬约束）
- Codex 数据目录 `~\.codex` 保持原位，不迁移，靠备份保命
- 游戏库统一 `D:\Games`，项目统一 `D:\Projects`
