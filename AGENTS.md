# 主管理：这台电脑与它下面所有项目的长期约定

`D:\AI` 是**主管理**（管家）：管这台电脑上所有项目的公共部分；它自己也带一个项目，就是本机的配置与维护。
项目一律放 `D:\Projects\<名字>\`，各自维护自己的 `状态.md`，并在 `项目登记表.md` 里登记一行。

对话可能被压缩、也可能开新会话，所以**状态一律以文件为准，不依赖对话记忆**。

## 最小必读（上下文紧张时至少读这一段）

1. **开工**：读 `任务.md`（要做什么）→ 读 `状态.md`（现在什么样）→ 动手
2. **红线**：数据放 D 盘 · 密钥不进任何仓库 · **删东西之前先问用户** · 不许动别人正在做的行
3. **收工**：更新 `任务.md` 那一行（做完就删行）→ 更新 `状态.md` →
   理由追加到 `决策记录\<当月>.md`，踩过的坑写进 `知识库\`
4. **杂活自己干，不要问**：归类、整理、清理、归档、更新索引
5. **六类事先问用户**：花钱 · 删除/不可逆 · 改偏好 · 对外发布/上传/发送 · 登录/密钥/权限 · 重启/安全策略/开机项/后台服务

## 这套东西是给会话用的，用户不会细看

他只有三件事：给决定、给钱、确认删除。

- 别写"请用户记得……"这类约定——他不会记得，那条约定等于不存在
- 归位、归类、清理、归档、索引，全部由会话做
- 发给用户的话要短：做成了什么、他能感知到什么、要他做什么
- 问他时**必须带默认方案**（"我打算这样，行不行"），不要让他从零想
- **但默认方案只在"开口问他"时用**：任务写明「等用户选定」的，**选定之前一步都不许动**——
  用户说「开始」只等于开始准备（调研、核实、把选项和代价摆出来），不等于替他选；
  装软件、删东西、改设置尤其算

## 任务就一个文件：`任务.md`

- `任务.md` **既是总览也是事实源**，直接改它。不再有 `任务\`、`进行中\` 认领卡、生成脚本
- 状态只用三个：**等你** / **在做** / **长期**（长期 = 不主动提，等指标越界或用户提起）
- **多会话同时干活时**：动手前把那一行改成「在做（会话标识 + 时间）」，做完改回来
- 做完的任务**直接从表里删掉**，历史看 `决策记录\`

## 决策、经验、现状各归各

- `状态.md` = **现在是什么样**（只写现在，不写流水账）
- `决策记录\<年-月>.md` = **为什么这么定**（只追加；要推翻旧结论就新增一条并注明推翻了哪条）
- `知识库\<主题>.md` = **怎么做**（可以改写、合并、改错）
- `决策记录.md` 只是索引

## 项目层级

- 主管理定**红线**：数据放 D 盘、密钥不进任何仓库、不许自作主张改安装位置
- **项目只继承两样：红线 + 一个"接手入口"**（项目级 `状态.md`，或 README 里固定一节写"做到哪、下一步"）。
  其余按项目自己的规范来——写代码的项目用代码世界的规矩（README、ADR、CHANGELOG、CI、测试），
  **不需要**照搬主管理这套任务与并发流程；单人单会话更不需要认领卡
- **每个项目的 `AGENTS.md` 第一行必须是「先读 `D:\AI\AGENTS.md`」**——项目不在 `D:\AI` 目录树下，
  不写这一句，Codex 在项目里就读不到主管理约定
- 新建项目：先建目录和约定文件，再在 `项目登记表.md` 加一行

## 文件体积与写作

- `状态.md` 只写「现在」，**超过 120 行就先压实**：留结论和还在生效的事
- `决策记录\` 只追加、不删；控制手段是「每条写得短」
- **不写密钥、令牌、账号密码**（这台机器上有 API 中转凭据，尤其注意）
- 不粘对话原文（原文在 `.codex\sessions\`）；每个文件保持简短

## 目录职责

- 个人文件（文章、资料、图片）→ `D:\Documents`；具体项目 → `D:\Projects\<名字>`；备份 → `D:\Backups`
- 判断不了放哪就先放 `收件箱\`，下次开工顺手归类
- 根目录允许放指向外部目录的快捷方式（不算治理文件）；下载的安装包可以留在下载目录
- **`产物\` 是临时产物区，用完即弃**（探针输出、当次日志），不做保留期管理

## 删除的红线

- **我自己刚产生的临时文件**（探针脚本、当次日志）→ 可以直接删
- **用户的东西、来历不明的东西** → **先问，不要替用户决定**

## 复用库

- **同一件东西用到第二次才收进库**；入库的在 `工具\工具箱.md` 占一行，三个月没用到的降级或删掉

## 仓库：只留私有仓

- **公开仓已于 2026-09-19 停用**。只保留私有仓 `pc-setup-private`，快照 `状态.md`、`决策记录\`、`任务.md`，
  同步用 `脚本\发布私有仓.py`
- **密钥、令牌、账号密码一律不进任何仓库**（私有仓也不行）；值放密码管理器（KeePassXC）
- 当初公开/私有的取舍与后来的简化，见 `决策记录\2026-09.md`

## 安装规则（装软件时必看）

- **装什么、从哪下载、密钥与订阅**：以 `文档\重装清单.md` 为准
- **装到哪**：以 `安装规划.md` 为准。清单只决定装什么，位置不由清单决定
- 顺序是硬性的：先 Clash Verge 并导入订阅，能上网了再装其余软件
- 装完一个就在 `状态.md` 记一行，并确认它的数据目录、库目录已经指向 D 盘
- 不要自作主张更换安装位置。要改先跟用户确认，改完同步更新 `安装规划.md`

## 本项目的既定约定（不要随意改动）

- C 盘（NVMe 512G）= 系统、软件、运行中的程序；D 盘（SATA 512G）= 数据、记录、备份
- 重装系统只清 C 盘，所以任何不能丢的东西都必须在 D 盘
- AI 主力走云端 API，不在本机跑大模型（显存 6GB、内存 16GB 是硬约束）
- Codex 数据目录 `~\.codex` 保持原位，不迁移，靠备份保命
- 游戏库统一 `D:\Games`，项目统一 `D:\Projects`
