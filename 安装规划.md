# 安装规划：装什么、装到哪

本文件决定**装到哪**。装什么、从哪下载、密钥与订阅地址，看 `D:\AI\文档\重装清单.md`。
两份文件配合使用，不要互相覆盖。

> **换电脑时的用法**：`文档\重装清单.md`（装什么）→ 本文件（装到哪）→ 装完记进 `状态.md`。
> 文档索引见 `文档\README.md`。

## 硬性顺序

1. Clash Verge → 导入订阅 → 开启系统代理
2. Codex、CC Switch（若已装则跳过）
3. 开发环境
4. 沟通与办公
5. 网盘与游戏
6. 驱动（NVIDIA / Dell），属于第 6 步，放最后并统一重启

## 两条总原则

- **程序本体装 C 盘**：都是「重装了还能装回来」的东西
- **数据、库、内容放 D 盘**：重装系统不会碰 D 盘

## 逐项对照表

### 代理与 AI 工具

| 软件 | 程序位置 | 数据位置 |
|---|---|---|
| Clash Verge | 已装：`C:\Program Files\Clash Verge` | 配置在 `%APPDATA%`；订阅配置不外传 |
| CC Switch | 已装：`%LOCALAPPDATA%\Programs\CC Switch` | 配置与密钥：`C:\Users\22707\.cc-switch` |
| Codex | 已装：`%LOCALAPPDATA%\OpenAI\Codex` | `C:\Users\22707\.codex`，**不迁移、不进 Git**；用户当前不做专门备份 |

### 安全与凭据

| 软件 | 程序位置 | 数据位置 |
|---|---|---|
| KeePassXC | `C:\Program Files\KeePassXC` | 加密密码库 → `D:\Documents\密码.kdbx`；主密码不得写进任何文件 |

### 开发环境

| 软件 | 程序位置 | 数据位置 |
|---|---|---|
| Git | `C:\Dev\Git`（或默认 `C:\Program Files\Git`） | 全局配置在用户目录 |
| Node.js | `C:\Dev\nodejs` | npm 缓存 → `D:\Dev\Cache\npm` |
| Python | `C:\Dev\Python` | pip 缓存 → `D:\Dev\Cache\pip` |
| MySQL 8.4 | C 默认 | **数据目录 → `D:\Dev\Data\MySQL`** |
| PostgreSQL | C 默认 | **数据目录 → `D:\Dev\Data\PostgreSQL`** |
| DBeaver | C 默认 | 连接配置在用户目录 |
| IntelliJ IDEA | C 默认 | 项目放 `D:\Projects` |
| Docker Desktop | C 默认 | **数据与镜像 → `D:\Virtual\Docker`**（用目录联接把 `%LOCALAPPDATA%\Docker` 指过去，见决策记录） |
| WSL | 系统自带 `wsl --install` | 发行版迁到 `D:\Virtual\WSL` |
| Logitech G HUB | C 默认 | — |
| PowerShell 7 | 已装：**Store/MSIX 版**（`C:\Program Files\WindowsApps\Microsoft.PowerShell_7.6.6.0_x64__8wekyb3d8bbwe`，命令别名 `%LOCALAPPDATA%\Microsoft\WindowsApps\pwsh.exe`） | — |
| Obsidian | 已装：`%LOCALAPPDATA%\Programs\Obsidian`（用户级） | 配置 `%APPDATA%\obsidian`；**库 = `D:\AI`**（拿来读 Markdown 文档）。`.obsidian\` 被白名单式 `.gitignore` 自动排除，不会进仓库 |

### 沟通与办公

| 软件 | 程序位置 | 数据位置 |
|---|---|---|
| 微信 / QQ | C 默认 | **文件与聊天记录 → `D:\Media\Chat`**（QQ 现在 `D:\Tencent Files`） |
| 腾讯会议 / 飞书 / 钉钉 | C 默认 | 录制与文件 → `D:\Media\Recordings` |
| 网易邮箱大师 / WPS Office | C 默认 | 文档在 `D:\Documents` |

### 网盘与游戏

| 软件 | 程序位置 | 数据位置 |
|---|---|---|
| 百度网盘 | C 默认 | 下载与同步目录 → `D:\Downloads` |
| Steam | C 默认 | **库目录 → `D:\Games\Steam`** |
| WeGame | C 默认 | **游戏目录 → `D:\Games\WeGame`**（实际落在 `D:\Games\WeGame\WeGameApps`） |

### 驱动（第 6 步，最后做）

| 项目 | 说明 |
|---|---|
| NVIDIA 显卡驱动 | 装到系统盘；和其他重启项一起，最后统一重启 |
| Dell BIOS / 驱动 | 刷新 BIOS 必须接通电源、不能中断，人工确认后执行 |

## 每次装完要做的三件事

1. 确认它落在了上表规定的位置
2. 把数据目录、库目录改成 D 盘（能改的当场改，别拖）
3. 在 `状态.md` 里记一行

## 目录现状（2026-09-18）

上面这些位置都已建好：`D:\Dev\Cache`、`D:\Dev\Data`、`D:\Virtual\WSL`、
`D:\Games\Steam`、`D:\Games\WeGame`，以及 `D:\AI` 下的中文分类目录。
Xbox 游戏目录的规划已取消（用户不玩 Xbox 内容）。
